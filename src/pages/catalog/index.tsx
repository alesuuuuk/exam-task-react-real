import React from 'react'

// styles
import s from './Catalog.module.scss'
// components
import ProductCard from '@/components/productCard'
import { ProdNoProps } from '@/interfaces'

const Catalog = (prods: ProdNoProps) => {
  return (
    <>
        
    </>
  )
}

export default Catalog